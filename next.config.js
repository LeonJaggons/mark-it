/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    output: "export",
    // loaders: [
    //     {
    //         test: /\.txt$/,
    //         use: ["text-loader"],
    //     },
    // ],
};

module.exports = nextConfig;
