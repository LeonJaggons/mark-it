/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    trailingSlash: true,
    // loaders: [
    //     {
    //         test: /\.txt$/,
    //         use: ["text-loader"],
    //     },
    // ],
};

module.exports = nextConfig;
